<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administración de Contactos - LoveDoc</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>

<body>
    <header>
        <h1>Panel de Administración - Contactos</h1>
        <nav>
            <ul class="nav-links">
                <li><a href="/">Inicio</a></li>
                <li><a href="/admin/contacts">📂 Contactos</a></li>
                <li><a href="/admin/payments">💳 Pagos</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <h2>Lista de Contactos</h2>

        <% if (typeof message !== "undefined" && message) { %>
            <p class="success"><%= message %></p> <!-- Mensaje de éxito -->
        <% } %>

        <% if (errors && errors.length > 0) { %> <!-- Manejo de errores -->
            <ul class="error-list">
                <% errors.forEach(error => { %>
                    <li class="error-text">⚠ <%= error %></li>
                <% }); %>
            </ul>
        <% } %>

        <table class="contacts-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Email</th>
                    <th>Comentario</th>
                    <th>IP</th>
                    <th>Fecha</th>
                </tr>
            </thead>
            <tbody>
                <% if (contacts && contacts.length > 0) { %> <!-- Asegurar que `contacts` tiene datos -->
                    <% contacts.forEach(contact => { %>
                        <tr>
                            <td><%= contact.id %></td>
                            <td><%= contact.nombre %></td>
                            <td><%= contact.email %></td>
                            <td><%= contact.comment %></td>
                            <td><%= contact.ip %></td>
                            <td><%= contact.date %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="6">No hay contactos registrados.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </main>

    <footer>
        <p>&copy; 2025 LoveDoc. Todos los derechos reservados.</p>
    </footer>
</body>
</html>